# Slack App credentials for the "Add to Slack" OAuth flow (see api.slack.com/apps).
# Needs the incoming-webhook Bot Token Scope and a redirect URL of
# {origin}/api/integrations/slack/callback registered for each environment.
SLACK_CLIENT_ID=
SLACK_CLIENT_SECRET=
